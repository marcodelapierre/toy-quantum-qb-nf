profiles {

  standard {
    process {
      executor = 'local'
      container = 'registry.gitlab.com/qbau/software-and-apps/public/qbsdk/qristal-sdk'
    }
    docker.enabled = true
    docker.runOptions = "--entrypoint=''"
  }

}
